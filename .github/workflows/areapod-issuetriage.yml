name: Area Pod Issue Triage

on: workflow_dispatch

jobs:
  areapod-issuetriage:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        pod: ["adam/david", "buyaa/jose/steve", "akhil/carlos/viktor"]

    steps:
      - name: Area Pod Issue Triage
        uses: jeffhandley/action-playground/actions/AreaPod/AreaPod.IssueTriage
        with:
          pod: "${{ matrix.pod }}"
