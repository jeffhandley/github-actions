name: "Publish: backlog"

on:
  push:
    branches: [ main ]
    paths: [ 'src/Backlog/**', 'src/Dockerfile' ]

  pull_request:
    branches: [ main ]
    paths: [ 'src/Backlog/**', 'src/Dockerfile' ]

  workflow_dispatch:

jobs:
  publish-action:

    permissions:
      contents: read
      packages: write

    uses: ./.github/workflows/publish-action.yml

    with:
      action_name: backlog
      action_folder: Backlog
